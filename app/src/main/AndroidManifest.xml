<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.yuevision">
    <!-- 权限设置 -->
    <!-- 相机权限 -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- universal-image-loader-1.9.2.jar两个权限之一 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.BROADCAST_STICKY" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- universal-image-loader-1.9.2.jar两个权限之二 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_DOWNLOAD_MANAGER" />
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <application
        android:name="com.yuevision.tools.MyApplication"
        android:allowBackup="true"
        android:largeHeap="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:theme="@style/AppTheme">
        <!-- 登录界面 -->
        <activity
            android:screenOrientation="portrait"
            android:name="com.yuevision.LoginActivity"
            android:label="@string/app_name" >
        </activity>
        <!-- 注册界面 -->
        <activity
            android:name="com.yuevision.RegisterActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 截图界面 -->
        <activity
            android:name="com.yuevision.PhotoClipPictureActivity"
            android:label="@string/app_name" >
        </activity>
        <!--&lt;!&ndash; 过度界面 &ndash;&gt;-->
        <!--<activity-->
            <!--android:name="com.yuevision.NoneActivity"-->
            <!--android:label="@string/app_name" >-->
        <!--</activity>-->
        <!-- 自定义拍照界面 -->
        <activity
            android:name="com.yuevision.MyCameraActivity"
            android:label="@string/app_name" >
        </activity>
    </application>

</manifest>